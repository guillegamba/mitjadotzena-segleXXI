export interface FeatureProps {
  title: string;
  description: string;
  imageUrl: string;
}

export interface UseCaseProps {
  title: string;
  description: string;
  outcome: string;
  ctaText: string;
  image: string;
}

export interface StepProps {
  number: string;
  title: string;
  description: string;
}